- include_javascript 'admin/dropdown'
- unless simple
  %td.actions
    = link_to image('plus') + ' ' + t('add_child'), admin_factory_link_path(:page => page), :class => 'action dropdown'
    = link_to image('minus') + ' ' + t('remove'), remove_admin_page_url(page), :class => "action"